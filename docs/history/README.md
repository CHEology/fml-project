# Historical Documents

This folder contains archived course-planning documents that describe how the
project evolved. They are retained for context, but they are not the current
source of truth for repository layout or implementation guidance.

Use the root `README.md`, package READMEs, and `AGENTS.md` for current guidance.
